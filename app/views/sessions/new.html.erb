<%= form_for @user, url: login_path, method: :post do |f| %>
  <%= f.label :name, "Your name:" %>
  <%= f.collection_select :name, User.all, :name, :name, include_blank: true %>

  <%= f.label :password %>
  <%= f.password_field :password %>

  <%= f.submit "Log In" %>
<% end %>
