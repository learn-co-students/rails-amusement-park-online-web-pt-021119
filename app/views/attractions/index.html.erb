<%= @attractions.each do |a| %>
  <%= content_tag(:p, (link_to "Go on #{a.name}", attraction_path(a))) if !@user.admin %>
  <%= content_tag(:p, (link_to "Show #{a.name}", attraction_path(a))) if @user.admin %>
<% end %>

<%= link_to "New Attraction", new_attraction_path if @user.admin %>