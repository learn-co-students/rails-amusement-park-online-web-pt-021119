<% @attractions.each do |attraction| %>
    <%= content_tag(:p, (link_to "Go on #{attraction.name}", attraction_path(attraction))) if !@user.admin %>
    <%= content_tag(:p, (link_to "Show #{attraction.name}", attraction_path(attraction))) if @user.admin %>
    
    <% end %>
    
    <%= link_to "New Attraction", new_attraction_path if @user.admin %>
    