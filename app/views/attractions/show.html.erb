<p><%= @attraction.name %></p>
<%= form_for @user do |f|%>
  <%= f.text_field :attraction_id, type: "hidden", value: "#{@attraction.id}" %>
  <%= f.submit 'Go on this ride' %>
<% end %>

<%= link_to "Edit Attraction", edit_attraction_path if @user.admin %>